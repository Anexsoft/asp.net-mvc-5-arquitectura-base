namespace Common
{
    public class Enums
    {
        public enum Gender
        {
            Male,
            Female
        }

        public enum Status
        {
            Enable,
            Disable
        }

        public enum MyFilters
        {
            IsDeleted
        }
    }
}
